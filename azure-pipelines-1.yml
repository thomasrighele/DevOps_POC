# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: 'ubuntu-latest'



steps: 
- task: CmdLine@2
  name: Copy
  inputs:
    script: 'cp $(System.DefaultWorkingDirectory)/testpush.txt $(System.DefaultWorkingDirectory)/TEST/testpush.txt'

- task: CmdLine@2
  inputs:
    script: |
        git checkout -b master

        git config --global user.email "thomas.righele@guycarp.com"
        git config --global user.name "Thomas.Righele"
        git status


        git add .

        git status

        git commit -m "copy test"

        git status

        git push https://Thomas.Righele:wsm7mv4mjik3zxjlyarwyvmfbgx6n3wsjxdenit5y5tqrwyjchkq@guycarp.visualstudio.com/GC%20Integration/_git/IICS%20DevOps origin master

      
      


