# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

steps: 
- task: CmdLine@2
  name: Copy
  inputs:
    script: 'cp $(System.DefaultWorkingDirectory)/testpush.txt $(System.DefaultWorkingDirectory)/TEST/testpush.txt'


- task: CmdLine@2
  name: Git_Add
  inputs:
    script: 'git config --global user.email "thomas.righele@guycarp.com" && git config --global user.name "Thomas.Righele" && git add . && git commit -m "update"'


- task: CmdLine@2
  name: Git_Push
  inputs:
    script: 'git push origin master' 


