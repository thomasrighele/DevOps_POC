# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: CmdLine@2
  inputs:
    script: 'git clone "https://guycarp.visualstudio.com/GC%20Integration/_git/GC%20Integration"'

- task: CmdLine@2
  inputs:
    script: 'dir $(System.DefaultWorkingDirectory)/GC%20Integration"'
