---
    - hosts: localhost
      connection: ssh
      gather_facts: yes
    
      vars_files:
        - group_vars/{{ app_environment }}.yml
        - group_vars/{{ app_name }}.yml
    
      roles:
        - { role: 'roles/informatica', action: "{{ action }}" }